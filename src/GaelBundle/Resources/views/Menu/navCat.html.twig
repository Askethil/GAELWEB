{# empty Twig template #}
{% block head %}
  <link href="{{asset ('css/navCat.css')}}" rel="stylesheet" type="text/css"/>
{% endblock %}
{% extends 'FancyBundle:Templates:Template.html.twig'%}
{% block article %}

<div class="col-md-12">
        <div id ="menuActicle" class="centrage" >
             {% set i = 1 %}
            {% for c in categories %} 
             <div class="col-md-3">
                <div id ="Article" >
                    <a href="{{path('articles',{idCat:c.idCat})}}"> <img  id="imageMenu" src="{{asset('images/'~c.imageUrl)}}"  width="200" height="200" alt></a>
                <div id ="titreArticle" >{{c.nom }}</div>
                </div>
                </div>
            {% set i = i+1 %}
            {% endfor %}
        </div>

 </div>
</div>
{% endblock %}
